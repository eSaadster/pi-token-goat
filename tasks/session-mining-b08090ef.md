# Session Mining: b08090ef

Mined from session b08090ef-508d-4cec-a214-14e6761d7b6c (actual path: `C:\Users\zelys\.claude\projects\C--Projects\b08090ef-508d-4cec-a214-14e6761d7b6c.jsonl`).

Context: this was a multi-session PC-performance investigation ("PC is sluggish / so slow / Python processes") that root-caused token-goat as the primary CPU/RAM offender on Windows. It produced a new `hook_relay.py` module, worker reindex tuning, a project-root blocklist, a Hermes Agent install integration, and a batch of CI/test-hook fixes. Most items below were implemented in-session; the ones still open are called out explicitly.

## Bug Fixes Needed

- [ ] Fix the `stats prune failed (global.db unavailable): attempt to write a readonly database` error seen repeatedly in the daemon log (`logs/2026-06-20.log`). The periodic stats-prune path treats `global.db` as readonly and silently fails every cycle. Locate the stats prune routine in `worker.py` and either open the DB writable, handle the locked/readonly case with a retry, or downgrade the noise. This was observed but never addressed in-session. — `worker.py` (stats prune), `global.db`
- [ ] Resolve the cmd.exe relay stdin/fallback double-dispatch edge case Codex flagged: if `curl` reads stdin then the relay times out (slow relay), `pythonw` is invoked with empty stdin and returns `{"continue": true}` — a silent no-op dispatch rather than the real hook result. Acknowledged as "valid but low-impact on localhost" and left unfixed. Decide whether to guard the fallback so a partially-consumed stdin doesn't produce a benign-but-wrong response. — `tg-hook.cmd` generator in `paths.py::hook_wrapper_content()`, `hook_relay.py`
- [ ] Verify the final landed state of the Hermes Agent integration on `main`. The user `git revert`-ed both Hermes commits mid-session while the test suite ran; the implementation was then re-added by hand (10 edits to `install.py`, 6 to `cli.py`, new `tests/test_hermes_install.py`). Confirm `install.py`, `cli.py`, and the test file actually contain the Hermes code on the current branch and that `--hermes` / `--target hermes` / `--target all` all work end-to-end; re-land as 1.9.6 if it was shipped reverted. — `install.py`, `cli.py`, `tests/test_hermes_install.py`

## Deferred Improvements

- [ ] Make the project-root blocklist (`_is_blocked_root`) extensible/configurable instead of a hardcoded constant. Currently `c:/Windows`, `node_modules` dirs, and `AppData/Temp` dirs are baked into a constant checked in both `worker.py::_reindex_active_projects` and `parser.py::index_project`. Users with other never-index roots (e.g. the `coracrea-website` project that timed out every cycle) have no way to exclude a specific project without editing source. Add a `WorkerConfig` key (e.g. `blocked_roots` / `exclude_projects`) or a `token-goat config` subcommand to exclude a project root from indexing. — `worker.py`, `parser.py`, `config.py`
- [ ] Add a `token-goat` CLI affordance to list and prune tracked projects from `global.db`. The session had to hand-edit `global.db` via raw `sqlite3` to remove 16 junk project entries (temp dirs, `c:/Windows`, node_modules roots, dead OSS repos). There is no first-class way to inspect or purge the `projects` table. — `cli.py`, `global.db` `projects` table
- [ ] Prevent junk roots from ever being registered as projects in the first place. `c:/Windows` (9,546 files) got registered because a session had its CWD there; temp dirs and a `node_modules/@uniswap/token-lists` subtree were also registered as standalone project roots. The blocked-root guard now filters at reindex/index time, but registration itself should reject these. Audit the project-registration path (where `last_seen`/`root` rows are inserted) and apply the blocklist there too. — project registration in `worker.py` / `hooks_*`

## Test Gaps

- [ ] Audit all test files that call `_insert_stat` / `_make_project` / write to the real data dir for the same isolation bug fixed in `test_importance_trim.py`. Root cause: tests persisted to the real `~/.local/share/token-goat/projects/` instead of a tmp dir; on WSL, `/tmp` clears between sessions but `$HOME` does not, so a recreated `pytest-0` path hashes to a stale DB and row counts accumulate (2x scores, non-empty where `{}` expected). Fixed via a module-level autouse `tmp_data_dir` in that one file, but `test_fallback_to_static_when_no_scores` and any other `_insert_stat` caller is still at risk. Add the autouse `tmp_data_dir` fixture (or a shared conftest fixture) everywhere stats DBs are written. — `tests/test_importance_trim.py` and siblings, `tests/conftest.py`
- [ ] Add a regression test asserting the pre-push lefthook globs actually fire on doc-only pushes. The `**/*.py` glob meant the hooks only ran when Python files changed, so a prior doc-only push slipped through CI-equivalent checks entirely (broken for ~5 days). After fixing the glob, lock it with a test or a comment documenting why the glob must not be path-restricted. — `lefthook.yml`, `.lefthook/pre-push/*`
- [ ] Close the two minor test gaps the code-reviewer flagged on the Codex/Gemini/Hermes install integrations: `verify_install()` OSError path and the partial-block replacement edge case. Noted as "pre-existing in Codex tests too, not blocking." — `tests/test_hermes_install.py`, install integration tests

## Performance

- [ ] (DONE in-session — verify it shipped) Periodic reindex was scanning all ~45-56 tracked projects every 10 min with a 7-day active window, producing 50-200s cycles and a runaway daemon burning 1,800+ CPU-seconds. Changed `PERIODIC_REINDEX_INTERVAL` 600s -> 1800s and `PERIODIC_REINDEX_ACTIVE_WINDOW` 7 days -> 4 hours, and made both env/config-overridable (`periodic_reindex_interval_secs`, `periodic_reindex_active_window_secs`). Confirm the config keys are wired through `config.py::WorkerConfig` -> parser -> `worker.py` and documented. — `worker.py` (~lines 125/143, `_reindex_active_projects`), `config.py`
- [ ] (DONE in-session — verify it shipped) Per-hook `pythonw.exe` spawn (200-500ms each, 30+ concurrent processes per Claude response, triggering Bitdefender on-access scans) replaced by an in-process `ThreadingHTTPServer` relay on `127.0.0.1:0`, with `tg-hook.cmd` POSTing via `curl` (3-10ms) and falling back to `pythonw` only when the relay is down. Confirm `hook_relay.py`, the `paths.py` wrapper generator, and `worker_daemon.py` lifecycle wiring are all on `main`. — `hook_relay.py`, `paths.py::hook_wrapper_content()`, `worker_daemon.py`
- [ ] Investigate why `index_project` times out at 30s on token-goat itself and on `coracrea-website` every cycle. The 30s timeout fires, the cycle retries, and CPU burns continuously. Either raise/scale the per-project timeout by file count, skip projects that repeatedly time out (circuit-breaker), or surface a warning so the user can exclude them. — `worker.py` `index_project` timeout path
- [ ] Handle very large files in the indexer more gracefully. The worker log showed repeated skips of a 169MB `pdp-lighthouse.json` and 12MB Shopify files every cycle — the skip decision is re-evaluated every pass rather than cached. Cache the "too large, skip" verdict per file SHA so the walk doesn't re-stat/re-skip giant files every cycle. — `worker.py` / `parser.py` file-walk

## Documentation

- [ ] (DONE in-session — verify) CHANGELOG `[Unreleased] -> Performance` and README performance table updated for the relay (new "Hook process spawn (Windows)" row, 200-500ms -> 3-10ms). Confirm both are present and the version bump (1.9.4 -> 1.9.5) closed the Unreleased block correctly.
- [ ] Document the `.python-version` pin gotcha somewhere developer-facing (CLAUDE.md or CONTRIBUTING). The repo's `.python-version` was `3.12`, which silently made every `uv sync` / `uv venv` rebuild the venv at 3.12 regardless of `--python 3.13` flags — a multi-hour rabbit hole. A one-line note ("`.python-version` governs the venv Python; change it, not just the flag") would save future debugging. — `CLAUDE.md` or `.python-version` comment
- [ ] Document the Windows venv-launcher redirect behavior for contributors: `.venv\Scripts\pythonw.exe` is a stub that exec's into the base Python named in `pyvenv.cfg`'s `home`, so spawned processes appear as the base interpreter (e.g. `C:\Python312`) in the process list even when launched via the venv path. This caused repeated misdiagnosis of "Python312 daemon respawning." — `CLAUDE.arch.md` design-decisions section

## Architecture / Design

- [ ] Add a relay-thread liveness self-heal beyond the current 60s check (partially DONE: `check_relay_liveness()` restarts the server if `is_alive()` is false, wired into `worker_daemon.py` at `_RELAY_LIVENESS_INTERVAL = 60s`). Confirm the port file is rewritten on restart so it never "silently points at a closed socket," and consider a stale-port-file detector on the `tg-hook.cmd` side (curl to a dead port should fail fast to fallback). — `hook_relay.py`, `worker_daemon.py`
- [ ] Harden `cleanup-orphans.ps1` against the failure mode that started this whole investigation: it originally only killed `worker --daemon` orphans, not stale `hook pre-*/post-*` processes, and only matched non-venv Python. It was extended in-session (kills stale hooks with a 30-min age floor, scoped by venv path). Consider folding this orphan-reaping logic into a `token-goat worker --kill-orphans` command so it isn't a separate PowerShell script the user must maintain. — `C:\Users\zelys\.claude\scripts\cleanup-orphans.ps1`, `worker.py`
- [ ] Reconsider the relay `Content-Length` cap of 1,048,576 bytes (1MB) in `hook_relay.py::do_POST`. Hook payloads are normally tiny, but a large pasted/piped payload would be silently truncated. Confirm 1MB is comfortably above the largest real Claude Code hook payload, or make it configurable. — `hook_relay.py`
- [ ] The `spawn_detached()` daemon-spawn path resolves the interpreter via `paths.python_runner_argv()` (`sys.executable` -> sibling `pythonw.exe`). Because of the venv-launcher redirect, this is correct only when the venv's `pyvenv.cfg` points at the intended base Python. Add a startup self-check (or extend `worker --check` / `install --check`) that warns if the running daemon's interpreter version doesn't match the project's `.python-version` / expected base. — `worker.py::spawn_detached`, `paths.py::python_runner_argv`

## Notes

User-environment / release actions that are not code tasks but are load-bearing follow-ups from this session:

- PyPI publish of 1.9.5 is BLOCKED on a token. The session ended waiting for the user to set `PYPI_TOKEN` / paste a token so `uv publish` can run. The version bump and commits are on `main`; only the publish step is pending.
- Bitdefender on-access scanning was the single largest CPU contributor (~60-105% combined) and cannot be fixed programmatically (BD replaces Defender; `Get-MpPreference` fails). User must add these to BD exclusions via UI: `C:\Projects`, `C:\Users\zelys\.claude`, `C:\Users\zelys\AppData\Local\uv`, `C:\Users\zelys\AppData\Roaming\uv`.
- A reboot was recommended (10+ day uptime, WindowsTerminal at 1.85GB RAM).

CI / lefthook fixes already applied in-session (verify on `main`): `uv run pytest`/`uv run mypy` fail on Windows Git Bash with "Failed to canonicalize script path" — switched to `uv run python -m pytest` / `python -m mypy` in `.lefthook/pre-push/test.sh` and `typecheck.sh`; `.sh` scripts forced to LF via `.gitattributes` to survive Windows checkout CRLF conversion.

Tooling caveat observed (not a token-goat bug): multiple `Edit` tool calls silently failed to persist to disk during the session ("None of the Edit calls made it to disk"), forcing fallback to writing files via Python. Worth keeping in mind when validating that in-session edits actually landed.
